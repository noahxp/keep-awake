name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      # Run unit tests
      - name: Run tests
        run: swift run KeepAwakeTests

      # Release build
      - name: Build (release)
        run: swift build --configuration release

      # Assemble .app bundle
      - name: Assemble .app bundle
        run: |
          APP_DIR="$GITHUB_WORKSPACE/KeepAwake.app"
          mkdir -p "$APP_DIR/Contents/MacOS"
          cp .build/release/KeepAwake "$APP_DIR/Contents/MacOS/KeepAwake"
          cat > "$APP_DIR/Contents/Info.plist" <<'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
            "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>KeepAwake</string>
              <key>CFBundleIdentifier</key>
              <string>com.keepawake.app</string>
              <key>CFBundleName</key>
              <string>KeepAwake</string>
              <key>LSBackgroundOnly</key>
              <true/>
              <key>NSPrincipalClass</key>
              <string>NSApplication</string>
          </dict>
          </plist>
          EOF

      # Install create-dmg
      - name: Install create-dmg
        run: npm install -g create-dmg

      # Package as DMG (auto-appends /Applications symlink)
      - name: Package as DMG
        run: create-dmg KeepAwake.app

      # Publish GitHub Release with DMG attachment
      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: KeepAwake*.dmg
